<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.serokorean"
    tools:ignore="ImpliedTouchscreenHardware,MissingLeanbackLauncher,MissingLeanbackSupport">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_penguin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bottomNavigation.FAQ"></activity>
        <activity android:name=".bottomNavigation.Search" />
        <activity android:name=".bottomNavigation.Settings" />
        <activity android:name=".BeginnerOneWebview.MainWebviewTest" />
        <activity android:name=".deprecated.ChapterPages" />
        <activity android:name=".level.BeginnerOne" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>